Write a PRD for a caching layer. We need a Redis cluster with 3 nodes behind a HAProxy load balancer. The API should use a read-through cache pattern with TTLs of 5 minutes for user profiles and 1 hour for product catalog data. We'll use Redis Sentinel for failover and implement cache invalidation via pub/sub. The cache keys should follow the pattern `{entity}:{id}:{version}` and we need a Lua script for atomic read-modify-write operations. Deploy on Kubernetes with a StatefulSet and PVCs for persistence.
