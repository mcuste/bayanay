Rewrite this in caveman style: "In order to set up the development environment, you will first need to install Node.js version 18 or higher. After that, you should clone the repository from GitHub and then navigate into the project directory. Once you're there, run npm install to install all of the necessary dependencies. Finally, you can start the development server by running npm run dev."
