Create a C4 Context diagram for a SaaS analytics platform called "DataLens". Users of the system: Data Analysts (primary users — build dashboards and run queries), Business Executives (view-only dashboard consumers), API Developers (integrate DataLens into other tools via REST API). The DataLens system itself is the central system. External systems it integrates with: Salesforce CRM (DataLens pulls CRM data via REST), Snowflake Data Warehouse (DataLens queries data warehouse via JDBC), SendGrid (DataLens sends alert emails and scheduled reports), Auth0 (handles SSO and user authentication), Stripe (processes subscription billing).
