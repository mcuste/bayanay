Create a block diagram of our microservices architecture with 3 columns. Left column: API Gateway (full width). Middle column: Auth Service, User Service, Order Service, Payment Service (4 blocks stacked). Right column: PostgreSQL (spans 2 rows), Redis Cache, Message Queue (RabbitMQ). Show connections: API Gateway connects to all middle-column services. Auth Service, User Service, Order Service each connect to PostgreSQL. Order Service and Payment Service connect to Redis Cache. Order Service and Payment Service connect to Message Queue.
