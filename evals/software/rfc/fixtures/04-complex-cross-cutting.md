Write an RFC for adding multi-tenancy to our platform. Right now everything runs in a single-tenant mode. We need: (1) tenant isolation at the database level with row-level security, (2) tenant-aware authentication and authorization, (3) tenant-specific configuration and feature flags, (4) tenant routing at the API gateway, (5) tenant-scoped background job processing, (6) per-tenant billing and usage metering, and (7) tenant admin portal for self-service management. We have 12 microservices that all need tenant awareness. This is for our B2B pivot — we want to onboard enterprise customers who each need isolated environments.
