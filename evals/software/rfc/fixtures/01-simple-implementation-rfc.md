Draft an RFC for adding rate limiting to our REST API. We're running an Express.js backend with Redis already available. We're seeing abuse from scrapers hitting our public endpoints — about 200 requests/second from single IPs. We want per-IP rate limiting with configurable thresholds per endpoint group (public, authenticated, admin). This is an implementation decision, no architecture changes needed.
