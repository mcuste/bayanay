update docs/rfc/RFC-001-api-versioning.md

We decided on the open questions: maximum concurrent versions will be 2 (not 3 — ops agreed after we showed them the maintenance burden data). And we'll maintain separate OpenAPI specs per version since the annotation approach was too confusing in review.
